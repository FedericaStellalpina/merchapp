# merchapp

```
# Requirements
# Linux
sudo gem install sinatra
sudo gem install sqlite3

# MacOS
brew install ruby
brew gem install sinatra
brew gem install sqlite3


# Start the app:
ruby app.rb
```

```
# TODO:
- calculate fields
- have a delet button
- have a way to edit
- button for sold
```
